/**
 * Description: Loads global styles once for every browser-rendered Gatsby page.
 * Dependencies: Gatsby browser API and the global CSS file.
 * Code-customers: All pages and reusable components in src/.
 * Variables/origin: Import path points to the repository-owned global stylesheet.
 */
import "./src/styles/global.css";
